{
  "name": "@yousim/core",
  "private": true,
  "comment:private": "Not published. `yousim` is the only package that ships; bun build --target bun inlines this one into it.",
  "version": "0.1.0",
  "license": "AGPL-3.0-only",
  "description": "Core simulation logic for YouSim - provider-agnostic identity simulator",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./storage": "./src/storage/index.ts",
    "./contract": "./src/contract.ts",
    "./model": "./src/model.ts",
    "./pkce": "./src/pkce.ts",
    "./credentials": "./src/credentials.ts",
    "./config": "./src/config.ts",
    "./commands": "./src/commands.ts",
    "./models": "./src/models.ts"
  },
  "dependencies": {
    "@ai-sdk/anthropic": "^2.0.40",
    "@ai-sdk/openai": "^2.0.59",
    "ai": "^5.0.85"
  }
}
