{
  "name": "@yousim/api",
  "private": true,
  "comment:private": "Not published. `yousim` is the only package that ships; bun build --target node inlines this one into it.",
  "version": "0.1.0",
  "license": "AGPL-3.0-only",
  "description": "Local YouSim server: Elysia + SQLite, no auth",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "dev": "bun --watch run src/index.ts",
    "start": "bun run src/index.ts"
  },
  "dependencies": {
    "@elysiajs/cors": "^1.4.0",
    "@elysiajs/node": "1.4.5",
    "@elysiajs/static": "^1.4.10",
    "@yousim/core": "workspace:*",
    "elysia": "^1.4.13"
  },
  "devDependencies": {
    "bun-types": "latest"
  }
}
